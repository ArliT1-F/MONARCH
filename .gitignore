# dependencies
node_modules/

# builds
.next/
dist/
out/
build/
*.tsbuildinfo

# runtime data (demo-mode file store)
.monarch-data/

# managed helper binaries (yt-dlp / ffmpeg downloaded by `npm run
# music:setup` or on the first /music play) and their cache
.monarch/

# env
.env
.env.local
.env.*.local

# logs
*.log
npm-debug.log*

# misc
.DS_Store
coverage/
.turbo/

# prisma generated client (npm run db:generate / postinstall)
apps/dashboard/lib/generated/

# images for the `!era zhvishu` easter egg — personal, never commit these
/era_img/*
!/era_img/README.md
