import { ComingSoon } from "@/components/ui/ComingSoon";

export default function Page() {
  return (
    <ComingSoon
      title="Design Analyzer"
      phase="Phase 7"
      description="Scores your server’s organization, naming and branding consistency, and suggests cleanups. Suggestions only — never automatic changes."
    />
  );
}
