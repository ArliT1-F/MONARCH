import { ComingSoon } from "@/components/ui/ComingSoon";

export default function Page() {
  return (
    <ComingSoon
      title="Branding Studio"
      phase="Phase 5"
      description="Centralized server branding: colors, role palettes and reusable visual themes that propagate to every Monarch feature."
    />
  );
}
